# Fine-grained PAT with read-only Contents access to asa1984/asa1984.dev-content.
# Leave empty and run with ALLOW_EMPTY_CONTENT=1 to work without content.
CONTENT_GITHUB_TOKEN=
FRONTEND_URL=http://localhost:3000
FRONTEND_API_TOKEN=example-token
