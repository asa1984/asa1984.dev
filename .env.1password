# 1Password CLI 経由で実コンテンツ付きローカル開発をするための参照定義。
# 値そのものは含まれない(op:// 参照のみ)。
#   op run --env-file=.env.1password -- pnpm run dev
ALLOW_EMPTY_CONTENT=0
CONTENT_GITHUB_TOKEN=op://asa1984.dev/dev/CONTENT_GITHUB_TOKEN
